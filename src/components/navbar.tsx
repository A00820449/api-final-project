import {AppBar, Toolbar, Box} from "@mui/material"

const NavBar = () => {
    return (
    <AppBar position='sticky'>
        <Toolbar>
            <Box>
                App
            </Box>
        </Toolbar>
    </AppBar>
    )
}

export default NavBar